

// THE CALCULATOR

//PART 1

var squareNumber = function ( x,y ) {
  var result = (x * x) ;
    console.log("The result of squaring the number " + x + " is " + result);
      return result;
};

squareNumber( 3,6 );

var halfNumber = function ( Num1, Num2 ) {
  var result = (Num1 / Num2) ;
    console.log("Half of " + Num1 + " is " + result);
    return result;
};

halfNumber( 5,2 );

var percentOf = function ( x,y ) {
  var result = ( 100 / y * x) ;
    console.log( x + " is " + result + "%" );
      return result;
};

percentOf(2,4);

areaOfCircle = function ( radius, area ) {
    var result = ( area * radius * 3.14)
      console.log( "The area for a circle with radius " + area + " is " + result );
        return result;
};

areaOfCircle(2,2);

// PART 2

var halfNumber = function ( x ) {
  var squareNumber = function ( radius * area )

}
